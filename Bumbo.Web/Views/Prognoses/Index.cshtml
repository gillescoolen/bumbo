@model IEnumerable<Bumbo.Data.Models.Prognoses>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AmountOfCustomers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmountOfFreight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WeatherDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Branch)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AmountOfCustomers)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AmountOfFreight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WeatherDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Branch.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
