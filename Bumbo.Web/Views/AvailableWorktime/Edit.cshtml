@model Bumbo.Data.Models.AvailableWorktime

@{
    ViewData["Title"] = "Edit";
}

<h2>Beschikbaarheid aanpassen</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="AvailableWorktime" asp-action="EditConfirmed">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="UserId" />
            <div class="form-group">
                <input type="hidden" asp-for="WorkDate" class="form-control" value="@Model.WorkDate" />
            </div>
            <div class="form-group">
                <label asp-for="Start" class="control-label">Starttijd</label>
                <input asp-for="Start" type="time" class="form-control" />
                <span asp-validation-for="Start" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Finish" class="control-label">Eindtijd</label>
                <input asp-for="Finish" type="time" class="form-control" />
                <span asp-validation-for="Finish" class="text-danger"></span>
            </div>
            @if (ViewBag.UserAge < 19)
            {
                <div class="form-group">
                    <label asp-for="SchoolHoursWorked" class="control-label"></label>
                    <input asp-for="SchoolHoursWorked" class="form-control" />
                    <span asp-validation-for="SchoolHoursWorked" class="text-danger"></span>
                </div>
            }
        <i>
            *Indien je niet beschikbaar bent, vul dan in:
            <br>
            01:00(AM) en 01:00(AM)
        </i>
            <div class="form-group">
                <input type="submit" value="Opslaan" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>



<div>
    <a asp-area="" asp-controller="AvailableWorktime" asp-action="Standard">Terug naar het overzicht</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
